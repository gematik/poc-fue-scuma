rootProject.name = "scuma"

pluginManagement {
    repositories {
        mavenLocal()
        //maven(url="https://repo.labor.gematik.de/repository/lab-gradle-plugins/")
        gradlePluginPortal()
    }
}